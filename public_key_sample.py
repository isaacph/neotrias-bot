
# PUBLIC_KEY = "big hash"
# APPLICATION_ID = "like 20 digit number"
# AUTH_TOKEN = "Bot big-base64-with-dashes"
# SERVER_HOSTNAME = "minecraft server address, probably the private IP if the bot is on the same VPC as the server"
# SERVER_RCON_PORT = (int) the RCON port in server.properties
# SERVER_RCON_PASSWORD = "RCON password in server.properties"
